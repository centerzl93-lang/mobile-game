# PLAYTEST.md — known issues & status

Known playtest issues, test flakiness, and open balance questions, with their current status. Every
item here is real and sourced from the code, the test suite, or `HANDOFF.md` — nothing speculative.
Balance items marked **needs-playtest** are deliberately *not* decided in code; they want real
playthroughs (or a player call), not a guess.

Status: 🔴 open · 🟡 mitigated / watching · 🟢 by-design (working as intended, flagged to watch) ·
⚪ won't-fix (documented workaround) · 🔵 decision needed.

---

## Test stability

| # | Issue | Status | Notes |
|---|---|---|---|
| T1 | **Path-confirm test flakes.** `tests/newgame.spec.ts` › "cancelling a drawn path clears it back to bare ground" (`@slow`) intermittently fails — `#confirm` bar not visible within the 5 s timeout. | 🔴 open | Camera/timing-sensitive. Passed on the identical tree in a prior run and on re-run; a re-run currently confirms it green. Top Phase-1 target — harden rather than re-run. |
| T2 | **"Walking-budget" flakiness pattern.** A cluster of `@slow` sim tests (household larders, trading-post stock order, mid-season merchant, breeding-without-housing, and `jobs & builders › with zero builders a site never builds; assigning builders constructs it` at `newgame.spec.ts:753`) have each failed once in a full run and passed alone. | 🟡 mitigated | Root cause: a villager/builder must *walk* somewhere within a step budget, which an unlucky random map breaks. Fix pattern: stand villagers where the work happens (`debugWorkSpot`) and count carried loads everywhere, not only in barns. Re-run a lone failure before believing it. **Observed live on `main` CI this session:** two different single-test failures on *unchanged* game code (T1's path-confirm on one run, this builders test on the next) — confirming the suite is intermittently red on `main` and making this the concrete Phase-1 target. |
| T3 | **Headless 3D is ~2 fps**, so click-driven specs are slow and can burn their budget. | 🟡 mitigated | UI/menu specs run on `?2d&gfx=low`; only renderer-assertion specs stay in 3D. Keep new click-through specs on `?2d`. |

## Simulation & survival

| # | Issue | Status | Notes |
|---|---|---|---|
| S1 | **Fuel delivery, not supply.** With the barn fall-back removed, a household its hauler never reaches can freeze in winter beside a full barn. | 🟢 by-design | Made together with `CONSUMPTION_SLOWDOWN` 3 (3× the woodpile runway). If villagers freeze beside full barns, look at `stockLarder` / the hauler round — not the fuel stock. Watch in Phase 1. |
| S2 | **Housing is the growth lever.** A couple needs a free house to form; grown children stay home until the player builds. | 🟢 by-design | Intended pacing. Watch that it reads as a clear "build houses" prompt, not as the village being stuck. |
| S3 | **A house of only children gets no larder** (no adult to run errands), so those children eat from the barns via the normal fallback. | 🟢 by-design | Harmless today; would disappear if home assignment kept children with a parent. |

## Balance (open questions — need playtesting/decision)

| # | Question | Status | Lever |
|---|---|---|---|
| B1 | **Do the big buildings cost too little?** 8×8 quarry / 6×6 mine / 5×9 trading post kept their small-footprint costs. | 🔵 decision | One-line edits in `BUILDING_DEFS`. Counter-argument: the land is now the real cost (finding the clear tiles). |
| B2 | **Work circles didn't grow with footprints** — big foraging buildings spend more of their circle on their own footprint, so yields sag a little. | 🔵 decision | A couple of lines in `workRadiusOf`, but it's a balance change wanting play. Fishing hut already handled via `dockDepth`. |
| B3 | **`CONSUMPTION_SLOWDOWN` = 3** cut demand by two-thirds with no supply-side change — the village may now be *too* easy to feed/heat. | 🟡 partly addressed | The **food** side was retuned (see **B6**: food consumption raised to 35, decoupled from this dial). This dial now governs **heat only**; whether winter fuel is too soft is still open. |
| B4 | **Difficulty is only a leg-up, not a ration** now (food/fuel/tools/coats identical across difficulties). Flattens the ladder. | 🔵 needs-playtest | If Normal must bite harder, adjust starting *materials* / `EASY_START_HOUSES`, not per-difficulty rations. |
| B5 | Other moved dials wanting play: housing capacity (8/10), `HOUSE_LARDER_SEASONS` (0.5) + ×3 basket, the `SEASON_BURN` table, birth rates (`BIRTH_CHANCE` 0.5 + surplus/wellbeing). | 🔵 needs-playtest | Balance review (Phase 8). |
| B6 | **Food was too generous — retuned to R=35.** Playtest: one Hunting Cabin + a partly-staffed Fishing Hut fed ~12 adults + 30 children (≈540 food/season). Production was measured (past the headless hauling stall, by teleport-delivering carried loads): **~142 food/worker/season, ~426 per 3-job cabin** — which reproduces the report exactly, so food was confirmed over-generous. At the old `FOOD_PER_CITIZEN_PER_SEASON` 20, one cabin fed ~21 adults (1 in 6 workers on food). **Change:** raised food consumption to **35/adult** (`FOOD_PER_CITIZEN_PER_SEASON = 35`, set *directly*, **decoupled from `CONSUMPTION_SLOWDOWN`** so winter fuel is untouched and the Phase-1 anti-freeze work stands). Effect: one cabin still carries the founding twelve (+81/season) but its ceiling is ~12 adults, so the first wave of births pushes a growing village past it and a **second food source is the thing to build before the 1200-food opening stock runs down** — food now scales with the village instead of being solved once. Everything downstream (larder targets, birth surplus gate, nomad threshold, low-stock floors) scales off the constant automatically. Guarded by a new sim test ("one food building sustains the founding village but not a grown one"). | 🟢 by-design | Watch in play that the second-building pressure reads as intended and not as an early-game wall (real cabins run below the idealised 426 — forest factor <1, housed workers lose time to errands — so the founding margin is thinner in practice; the opening stock absorbs it). If it bites too early, ease toward 30–32; too soft, push toward 40. Farms (~320/worker) keep late-game food efficient, so this is chiefly an early/mid-game pressure. Cross-ref B3. |
| B7 | **`NO_TOOLS_PENALTY` (0.6) was acting as a major production multiplier, not a modest one — retuned to 0.75.** Playtest report: with tools stocked, food needed ~50% of the workforce (the design's own anchor); once the barns ran dry, food needed ~75%, leaving too few hands for wood/firewood and killing the run before winter — a *temporary* tool shortage produced a near-unrecoverable cascade. The audit: no tool (0.6) vs iron (1.0) vs steel (1.15) is a 1.67× swing between "no tools" and "iron", applied to every non-farm trade (`workOutput`'s `tf` multiplies gatherer/fishing/hunting/lumberyard/quarry/mine/woodcutter/blacksmith/luxury/tailor output *and* `labour()`'s builder-work — farms alone are exempt, they answer to no chisel). A 1/0.6 ≈ 1.67× workforce cost to stand still (50% → 83%) is what a genuinely "modest" bonus should never do; a 1/0.75 ≈ 1.33× cost (50% → 66.7%) is a real bite that still leaves a third of the workforce for everything else. **Change:** `NO_TOOLS_PENALTY` 0.6 → 0.75 (matches `COLD_WORK_FACTOR`, this codebase's existing "going without, but still working" tier, rather than `COLD_WORK_MIN`'s crisis-grade 0.6). `IRON_TOOL_PROD` (1.0, the tuning anchor) and `STEEL_TOOL_PROD` (1.15 — already a modest, non-"overwhelming" edge, and the real reason to bother with steel is `STEEL_DURABILITY`'s 2×, unchanged) are untouched — the swing that needed narrowing was the bottom of the ladder, not the top. Difficulty's starting tool stockpiles (48 Normal / 24 Hard) are untouched too: the audit found the *penalty magnitude*, not the *starting quantity*, was the lever making a temporary shortage catastrophic, and the gentler penalty alone makes Hard's leaner 24-tool opening survivable without also making it a bigger handout. Guarded by new tests: the exact tier factors (`tier factor` test, updated for 0.75), a Normal village surviving three tool-less seasons without deaths, and Hard clearing its opening seasons on its own starting kit. | 🟢 by-design | The one thing this change *cannot* touch: once a village is self-sufficient in tools, Hard and Normal run identical production/consumption math (see B4) — difficulty is only ever the opening stockpile. A fully-tooled Hard is still strictly harder than Normal (half the starting food/fuel runway, tested), but it is no harder *by design* than that gap once both villages are self-sufficient; making it so would be a difficulty-system change, not a tool-balance one, and is out of this audit's scope. Watch in play whether the new 0.75/1.0/1.15 ladder still feels like it matters — if losing tools reads as too soft now, ease back toward 0.7; if a temporary shortage still spirals, push toward 0.8. |
| B8 | **Tools moved from a village-wide switch to a per-villager belonging**, on top of B7's value retune. The old model (`villageToolTier`) read *any* tool anywhere in the barns and lifted every worker in the village to that tier at once — the instant the last tool anywhere broke, every trade in the village dropped to `NO_TOOLS_PENALTY` simultaneously, a cliff-edge that made a single unlucky wear-out event look like a village-wide crisis even with, say, eleven of twelve workers still perfectly equipped. **Change:** each citizen now carries their own `tool` (`'iron' \| 'steel' \| undefined`) and `toolWear`. A bare-handed villager equips the next time they are naturally standing at a barn for another reason — dropping off a load, fetching a converter input, fetching or returning builder materials (`tryEquipTool`, steel first) — nobody is sent on a dedicated trip. Wear (`wearCitizenTool`) accrues onto *that villager's own* tool as they work, at the same per-cycle / per-builder-work rates as before, and clears it when it breaks. `citizenToolFactor(c)` replaces `toolProdFactor(s)` at every call site (`workOutput`, `labour`). Effect: a tool shortage is now a *gradient* across the workforce — some villagers equipped, some between barn visits — rather than a single switch, so the village's overall productivity degrades in proportion to how many tools are actually short, not in one lump the moment the barns hit zero. `workplaceStatus`'s "slowed" line and the villager sheet's Tool row both now read per-worker/per-citizen instead of the old village-wide fact. | 🟢 by-design | Watch two things in play: (1) whether the gradual, per-worker degradation reads as clearly to the player as the old all-at-once switch did — the workplace status line ("N/M bare-handed") is the intended replacement cue; (2) whether opportunistic-only pickup (no dedicated fetch trip) leaves some job types (ones that rarely visit a barn) staying bare-handed for a long stretch even when the barns are well stocked — if that shows up as a real problem, the fix is a dedicated low-priority "go get a tool" errand for a long-bare-handed villager, not reverting to the village-wide model. |
| B9 | **Steel tools carried their own stockpile-limit row, duplicating the plain-tool one.** The HUD chip and the villager's own kit (B8) already treat iron and steel as one "have we got tools" figure, but the stockpile-limits panel and `limitedOutput` still gave a steel-recipe smith its own `'steeltools'` cap, independent of the iron one — so a player could cap "Tools" at 50 expecting to slow the whole tool supply and find a steel smith still running flat out because its *separate* cap was still empty. **Change:** `limitedOutput` now answers `'tools'` for a blacksmith on either recipe, and `limitStock(s, 'tools')` reads the *combined* iron+steel total (mirroring `totalFood`'s pattern for its own category) rather than each kind separately; `steeltools` is dropped from `LIMITABLE` so the panel offers one "Tools" row instead of two. `s.limits.steeltools` on an old save is simply never read again — harmless leftover data, no migration needed. Guarded by a new test ("one tools cap covers iron and steel together, not one seam at a time") that caps `tools` at 50, fills it with plain iron alone, and confirms a *steel-recipe* smith still reads as at-limit. | 🟢 by-design | None outstanding — this folds the limit system into the same one-cap-for-both model the HUD and the tool ladder already used, rather than introducing a new concept. |

## UI / rendering

| # | Issue | Status | Notes |
|---|---|---|---|
| U1 | **Top-line HUD wraps to two rows** at 430px with the nine requested chips. | 🔵 decision | One row would need materially smaller chips or a horizontal scroll (which hides items on mobile). |
| U2 | **Per-crop field art draws generically.** `makeFarmField` (3D) now furrows the plot and grows a stand of crop clumps tinted by `cropDesign(b.crop).color`, ripening from green toward the crop's colour as `growth` climbs 0→1 — the "cheap color tint first" pass this row asked for. | 🟡 planned | 3D done; the 2D `drawFarm` still draws every crop the same generic tilled soil + emoji marker. Real per-crop *models* (the reserved `CropDesign.model` slot) remain future work. (Phase 7) |
| U3 | **3D ranch pen shows no live animal glyphs/count** (the 2D renderer does). | 🟢 done | `makeRanchPen` scatters low-poly critters (shape/size/colour by `RanchAnimal`) across the pen, count-capped and refreshed whenever the herd or species changes. The exact head count stays in the inspect sheet, same as before — this is the glyphs half of the gap, not a 3D count badge. |
| U4 | **Rotate-button direction.** Verified correct against the glyphs and covered by a test. | 🟢 by-design | If a player still wants it inverted after trying it, it's a one-line sign flip in `Game.rotateView` (+ the two rotate-suite assertions). |

## Placement / world

| # | Issue | Status | Notes |
|---|---|---|---|
| P1 | **Quarry placement reliability.** At 8×8 a big pit is hard to site on a small map; both quarry tests retry across up to 8 generated worlds to find a site. | 🟡 watching | If those start failing, the pit has outgrown the map (small maps went to 72 tiles a side to compensate) rather than the test going flaky. |
| P2 | **Old saves keep tile positions but pick up new (larger) footprints**, so a pre-resize village can have overlapping buildings or a covered door. | ⚪ won't-fix | Documented workaround: start a new game. Same as the earlier quarry resize; no migration machinery for one save. Tell the player if they hit it. |

## Housekeeping

| # | Item | Status | Notes |
|---|---|---|---|
| H1 | **Repo rename** `mobile-game` → `little-village` (manual, GitHub Settings). | ⏳ pending (user) | *After* rename, update `vite.config.ts` `BASE`, `playwright.config.ts` `BASE`, `README.md` URLs, and the `HANDOFF.md` Repo line, or Pages breaks. Package name already `little-village`. (Phase 9) |

---

*Update the status column as issues are resolved or decided. Add a new row when a real issue is
found in play — keep speculation out.*
